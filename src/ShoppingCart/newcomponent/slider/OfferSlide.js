import React from "react"
import { Button } from "react-bootstrap"
export const OfferSlide = ({data}) => {
    return (
        <div >
       <img src={data.image} alt="no pic" />
        </div>
    )
}

// export const WelcomeSlide = (title) => (
    // <p>{title.title}</p>
    // )